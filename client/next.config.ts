import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com', 'via.placeholder.com', 'media.istockphoto.com'], // Add your image domains here
  },
};

export default nextConfig;
